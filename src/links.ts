/**
 * Автор и его публичные ссылки.
 *
 * Живут в коде, а не в i18n: адреса одинаковы на любой локали, а имя
 * различается только транслитерацией — её и держит перевод. Это первые
 * и пока единственные внешние ссылки в приложении, поэтому у них
 * `rel="noreferrer"`: обе ведут на профили, и передавать туда заголовок
 * Referer с адресом приложения незачем.
 *
 * Зачем вообще в интерфейсе, а не только в LICENSE: приложение раздаётся
 * по HTTP отдельно от репозитория, и человек, открывший querium.pages.dev,
 * файлов лицензий не видит вовсе. Без этого у него нет ни способа узнать,
 * кто автор, ни способа связаться — при том что контент отдан под
 * CC BY-NC-SA, которая требует указания авторства при переиспользовании.
 *
 * Отдельным модулем, а не константой в App: те же адреса нужны подвалу
 * бокового меню (см. Sidebar), а импортировать их оттуда из App нельзя —
 * App сам импортирует Sidebar.
 */
export const AUTHOR_LINKEDIN = 'https://www.linkedin.com/in/stanislavsidorovich';
export const AUTHOR_REPO = 'https://github.com/StanislavSidorovich/Querium';

/**
 * Имя правообладателя — латиницей на обеих локалях, ровно той строкой, что
 * стоит в LICENSE и LICENSE-CONTENT («Copyright 2026 Stanislav Sidorovich»).
 * Русская форма имени в прозе «О тренажёре» этому не противоречит: там текст
 * о человеке, здесь — юридический нотис, и он обязан совпадать с файлом
 * лицензии буква в букву, иначе при переиспользовании контента непонятно,
 * какое из двух написаний указывать в атрибуции.
 */
export const AUTHOR_NAME = 'Stanislav Sidorovich';

/**
 * Год копирайта — константой, а не `new Date().getFullYear()`. Год в нотисе
 * означает год выпуска, а не сегодняшнюю дату: подставляя текущий, приложение
 * начало бы расходиться с LICENSE ровно в новогоднюю ночь и без единой правки
 * в репозитории.
 */
export const COPYRIGHT_YEAR = 2026;
